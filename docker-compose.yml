version: '3.8'

# Docker Compose PRINCIPAL para desarrollo local
# USO:
#   docker-compose up
#
# NOTA: SQL Server se ejecuta en local (Windows)
# Devs corren aplicación localmente:
#   - cd backend && dotnet run --project src/QuickMeet.API
#   - cd frontend && npm start

services:
  # ====================================
  # API Backend - OPCIONAL
  # Los devs corren esto LOCALMENTE:
  #   cd backend
  #   dotnet run --project src/QuickMeet.API
  # ====================================
  # api:
  #   build:
  #     context: ./backend
  #     dockerfile: src/QuickMeet.API/Dockerfile
  #   container_name: quickmeet-api
  #   environment:
  #     ASPNETCORE_ENVIRONMENT: Development
  #     ASPNETCORE_URLS: "http://+:8080"
  #     ConnectionStrings__DefaultConnection: "Server=sql-server,1433;Database=QuickMeet;User Id=quickmeet_app;Password=QuickMeet$App.Secure2025!;TrustServerCertificate=true;"
  #   ports:
  #     - "5000:8080"
  #   networks:
  #     - quickmeet
  #   # Hot reload: descomentar si quieren cambios en tiempo real
  #   # volumes:
  #   #   - ./backend/src:/app/src
  #   #   - /app/bin
  #   #   - /app/obj

  # ====================================
  # Frontend - OPCIONAL
  # Los devs corren esto LOCALMENTE:
  #   cd frontend
  #   npm start  (o ng serve)
  # ====================================
  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile.dev
  #   container_name: quickmeet-frontend
  #   environment:
  #     API_URL: "http://localhost:5173"
  #   ports:
  #     - "4200:4200"
  #   networks:
  #     - quickmeet
  #   # Hot reload: descomentar para cambios en tiempo real
  #   # volumes:
  #   #   - ./frontend/src:/app/src
  #   #   - /app/node_modules

networks:
  quickmeet:
    driver: bridge
