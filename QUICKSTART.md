# Para DB, por ahora se usa SQL SERVER en mi pc Windows, queda pendiente migrarla a una SQL SERVER en la nube

# Levantar Backend
cd Backend
dotnet run --project src/QuickMeet.API

# Levantar Frontend
cd Frontend
npm run start